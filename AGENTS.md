# Agent 工作手册

本仓库由多个 AI agent 协作维护。**开始任何工作前，先完整阅读本文件和 [docs/data-standards.md](docs/data-standards.md)。** 指标定义见 [docs/methodology.md](docs/methodology.md)。

## 你的角色

为一座中国主要城市收集、整理、更新十二个维度的结构化信息。你的产出会被用来做严肃的人生决策（迁居、买房、择业），因此：**准确 > 完整 > 快**。

## 核心规则（必须遵守）

1. **`cities.yml` 是城市清单与状态的唯一权威来源。** 任何状态变化（认领、完成、过期）都必须先改这里。
2. **一次只认领一个城市。** 只修改自己认领城市目录下的文件，外加四个共享文件：`cities.yml`、根 `README.md` 的状态表、`CHANGELOG.md`。共享文件只允许追加行或修改自己城市对应的行。
3. **不编造数据。** 找不到就留空并写 `TODO: 未找到（已尝试 XX 渠道）`；推算值必须标 ⚠️ 并写出推算方法。
4. **每个数字必须带年份和来源**（三元组原则，见数据规范）。
5. **更新不覆盖历史。** 刷新指标时，把旧值移入该文件的"历史数据"表；趋势本身就是本仓库最有价值的信息。
6. **宁可少而准，不要多而糊。** 一个有来源的中位数好过十个无来源的"大约"。

## 任务类型

### A. 建档：为尚无档案的城市创建并填写档案

1. **认领**：在 `cities.yml` 中找一个 `status: unclaimed` 的城市（优先 `priority: P0`），将其改为 `status: in-progress`，并把 `maintainer` 填为你的标识（如 `agent-shenzhen-2026q3`）。
2. **建骨架**：运行 `bash scripts/new-city.sh <path> <城市名> <省份名>`。`<path>` 取 `cities.yml` 中该城市 `path` 去掉 `cities/` 前缀，例如：
   ```bash
   bash scripts/new-city.sh fujian/xiamen 厦门 福建
   bash scripts/new-city.sh beijing 北京 直辖市
   ```
   建完骨架先把 `cities.yml` 状态改为 `scaffolded`，再开始填写。
3. **填写**：按 `01 → 12` 顺序逐维度填写。每个维度**先填完"核心指标"表，再写定性解读**；定性判断必须能被表中数据或明确引用的事实支撑。
4. **逐维度收尾**：每填完一个维度，更新该文件头部状态行（🔴→🟢）、文末"更新记录"表，以及 `meta.yml` 中对应条目。
5. **整城收尾**：12 个维度完成后——
   - 填写城市 `README.md`（一句话定位、关键数字速览、评分卡、一票否决检查）；
   - 整理 `sources.md`；
   - `cities.yml`：`status: complete`、`last_update` 填今天；
   - 根 `README.md` 状态表更新自己城市那一行（含链接）；
   - `CHANGELOG.md` 追加一行。
6. **提交**：每完成一个维度提交一次，信息格式 `<城市>: <动作> (<YYYY-MM>)`，例如 `厦门: 完成 04-housing (2026-09)`。

### B. 更新：刷新已有城市的过期数据

1. **选择目标**：在 `cities.yml` 中找 `last_update` 最旧或 `status: stale` 的城市认领（改 `maintainer`，`status: in-progress`）。若目标城市 `maintainer` 非空且 `last_update` 在 30 天内，说明有其他 agent 在工作，**换一个城市**。
2. **确定范围**：对照下方"更新节奏"表和城市 `meta.yml` 中各维度的 `updated` 日期，只刷新到期的维度，不要全量重写。
3. **刷新**：旧值移入"历史数据"表 → 填入新值（新年份、新来源）→ "更新记录"表加一行 → 更新 `meta.yml`。
4. **重大变化单独标注**：限购取消、地铁新线开通、落户政策放宽这类拐点信息，除了写入对应维度文件，还要在 `CHANGELOG.md` 中注明。
5. **收尾**：同建档第 5、6 步；`status` 改回 `complete`。

## 更新节奏

| 频率 | 维度 | 备注 |
|------|------|------|
| 每季度 | `04-housing`（价格/成交）、`02-industry`（招聘热度）、`12-policy` | 变化快 |
| 每半年 | `03-income`、`05-population`、`06-transport`、`10-governance` | |
| 每年 | `01-economy`、`07-education`、`08-healthcare`、`09-environment`、`11-lifestyle` | 统计公报每年 2–4 月发布，**Q2 集中刷新** |

过期判定（标 ⚠️ stale）：季度类数据超过 6 个月未更新；年度类数据超过 18 个月未更新。

## 数据收集渠道建议

按优先级排序（详见 [docs/data-standards.md](docs/data-standards.md)）：

1. **官方统计**：该市《国民经济和社会发展统计公报》、市统计局官网/统计年鉴、政府工作报告、国家统计局（70 城房价指数等）；
2. **部门与机构发布**：住建、教育、卫健、人社部门官网；中国城市轨道交通协会；《中国主要城市通勤监测报告》；
3. **权威研究机构**：贝壳研究院、中指研究院、克而瑞（房产）；智联招聘、BOSS 直聘（薪酬与招聘）；高德/百度地图（拥堵）；胡润/IT 桔子（独角兽、融资）；
4. **媒体与自媒体**：仅用于线索与定性佐证，数字尽量回溯到原始来源；
5. **估算**：万不得已时使用，必须标 ⚠️ 并写明方法。

## 质量自查清单（提交前逐项确认）

- [ ] 每个数值都有年份 + 来源，无裸数字
- [ ] 口径已注明（常住/户籍、平均/中位数、成交价/挂牌价、全体/城镇居民）
- [ ] 没有残留模板占位符（搜索 `{{` 确认为零）
- [ ] 定性判断有数据或事实支撑，观点与数据分开
- [ ] 文件头状态行、`meta.yml`、`cities.yml`、根 README 状态表、`CHANGELOG.md` 五处一致
- [ ] 更新任务：旧值已移入历史表而非被覆盖

## 禁止事项

- ❌ 编造或"合理猜测"任何数值
- ❌ 修改其他城市目录下的文件
- ❌ 覆盖删除历史数据
- ❌ 大段复制媒体文章原文（用自己的话总结并给出处）
- ❌ 在评分卡中给出没有维度文件支撑的分数
